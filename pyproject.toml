[project]
name = "blaise-fub-mcp"
version = "5.0.0"
description = "Private Follow Up Boss MCP operator for Blaise Smith"
requires-python = ">=3.11"
dependencies = [
  "mcp[cli]>=1.28,<2",
  "httpx>=0.27",
  "pydantic>=2.0",
  "PyJWT[crypto]>=2.8",
]
