[project]
name = "blaise-fub-mcp"
version = "0.2.0"
description = "Read-only authenticated MCP bridge for Blaise Smith's Follow Up Boss account"
requires-python = ">=3.10"
dependencies = [
  "mcp[cli]>=1.0",
  "httpx>=0.27",
  "pydantic>=2.0",
  "PyJWT[crypto]>=2.8",
]

[project.scripts]
blaise-fub-mcp = "server:main"
